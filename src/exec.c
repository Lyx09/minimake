#include "exec.h"

#include "parser.h"
#include "vector.h"

int exec_target(char *target, struct vector *targets, struct vector *vars)
{
    (void) target;
    (void) targets;
    (void) vars;
    // FIXME

    return 0;
}
