#include "parser.h"

int parse()
{
    /*
     * if line contain =
     *      line is var def
     *      store var
     * else if line contain :
     *      line is target
     *      store target
     *      while rule (\t comment or blank)
     *      
     * */
    return 0;
}
